---
version: '3.2'

services:

  test: &base
    build:
      context: .
      dockerfile: Dockerfile-dev
    working_dir: /app
    volumes:
      - ".:/app"
      - /app/node_modules
      - "$HOME/.kube:/root/.kube"
    entrypoint:
      - yarn
      - run
      - test

  sh:
    <<: *base
    entrypoint:
      - "bash"

  ntpl:
    image: ikerry/ntpl:v1.0.1
    working_dir: /app
    volumes:
      - ".:/app"
      - "$HOME/.kube:/root/.kube"
